version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: shtpage-web:latest
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
